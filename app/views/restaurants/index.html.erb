<div class="restaurant-cards my-3">
  <%= render @restaurants %>
</div>

<h2>Add a new restaurant</h2>
<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :tag_list, as: :check_boxes, collection: Restaurant::TAGS_LIST %>
  <%= f.submit %>
<% end %>


